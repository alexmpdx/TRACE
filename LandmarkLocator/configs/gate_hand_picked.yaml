confidence:
  # All landmarks default to the permissive globals. Strict per-landmark overrides
  # can be added here or via the GUI's Gate Config... dialog on a per-run basis.
  peak:
    global: 0.10
    per_landmark: {}
  sharpness:
    global: 1.15
    per_landmark: {}
  second_peak_ratio:
    global: 0.80
    per_landmark: {}
  second_peak_suppression_radius_px: 30
  core_landmarks:
    - subcostal_break
    - alula_notch
    - l1_rs
    - l2_l3
    - l4_l5
    - dtip
